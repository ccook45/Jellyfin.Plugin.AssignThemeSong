name: Jellyfin 12 build and release

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Build
        run: dotnet build Jellyfin.Plugin.AssignThemeSong.csproj --configuration Release

      - name: Package
        shell: bash
        run: |
          rm -rf package
          mkdir package
          cp -a bin/Release/net10.0/. package/
          cd package
          zip -r ../xThemeSong_v1.4.15.zip .

      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh release view v1.4.15 >/dev/null 2>&1; then
            gh release upload v1.4.15 xThemeSong_v1.4.15.zip --clobber
          else
            gh release create v1.4.15 xThemeSong_v1.4.15.zip --title "xThemeSong 1.4.15 - Jellyfin 12" --notes "Expand opening theme search coverage and return more candidate videos."
          fi

      - name: Update manifest
        shell: bash
        run: |
          python3 - <<'PY'
          import hashlib, json
          from pathlib import Path

          asset = Path("xThemeSong_v1.4.15.zip")
          checksum = hashlib.md5(asset.read_bytes()).hexdigest().upper()
          path = Path("manifest.json")
          data = json.loads(path.read_text(encoding="utf-8-sig"))

          entry = {
              "version": "1.4.15.0",
              "checksum": checksum,
              "changelog": "v1.4.15: Fix Jellyfin 12 thumbnail authentication and improve theme-focused YouTube search.",
              "targetAbi": "12.0.0.0",
              "sourceUrl": "https://github.com/ccook45/Jellyfin.Plugin.AssignThemeSong/releases/download/v1.4.15/xThemeSong_v1.4.15.zip"
          }

          for plugin in data:
              plugin["owner"] = "ccook45"
              versions = plugin.setdefault("versions", [])
              versions[:] = [v for v in versions if v.get("version") != "1.4.15.0"]
              versions.insert(0, entry)

          path.write_text(json.dumps(data, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
          print(f"Manifest checksum: {checksum}")
          PY

      - name: Commit manifest
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add manifest.json
          if ! git diff --cached --quiet; then
            git commit -m "Publish Jellyfin 12 manifest"
            git push
          fi
